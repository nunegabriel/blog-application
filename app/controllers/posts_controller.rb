class PostsController < ApplicationController
  def index
        # user_id = params[:user_id]
    # Use the user_id parameter to fetch the relevant posts
    # For example: posts = Post.where(user_id: user_id)
    # Render the posts as a JSON response
    # render json: { posts: posts }
  end

  def show
    # user_id = params[:user_id]
    # post_id = params[:id]
  end
end
